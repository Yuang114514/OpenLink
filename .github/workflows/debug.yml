jobs:
  labeler:
    runs-on: ubuntu-latest
    steps:
      - name: Debug GitHub API Access
        run: |
          curl -H "Authorization: Bearer ${{ secrets.Crowdin_Merge }}" \
               -H "Accept: application/vnd.github.v3+json" \
               https://api.github.com/repos/${{ github.repository }}/contents/.github/labeler.yml